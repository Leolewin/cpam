<?xml version="1.0"?>
<packages>
  <package name="libunistring" version="0.9.3">
    <package-manager name="depot">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWWlibunistring09</pkgname-base>

      <version>0.9.3</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWlibiconv113"
          subpkg="runtime">v&gt;=1.13.1</depend>
      </subpkg>
    </package-manager>

    <package-manager name="inst">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWWlibunistring09</pkgname-base>

      <version>1</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWlibiconv113"
          subpkg="runtime">v&gt;=1</depend>
      </subpkg>
    </package-manager>

    <package-manager name="lpp">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWW.libunistring09</pkgname-base>

      <version>0.9.3</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWW.libiconv113"
          subpkg="runtime">v&gt;=1.13.1.1</depend>
      </subpkg>
    </package-manager>

    <package-manager name="pkgadd">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWWlibunistring09</pkgname-base>

      <version>0.9.3</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWlibiconv113"
          title="Iconv Library"
          subpkg="runtime">v&gt;=1.13.1</depend>
      </subpkg>
    </package-manager>

    <package-manager name="rpm4">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWWlibunistring09</pkgname-base>

      <version>0.9.3</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWlibiconv113"
          subpkg="runtime">v&gt;=1.13.1</depend>
      </subpkg>
    </package-manager>

    <package-manager name="setld">
      <category>Development/Tools</category>
      <title>GNU libunistring</title>
      <vendor>Bruno Haible</vendor>

      <description>
The 'libunistring' library implements Unicode strings (in the UTF-8,
UTF-16, and UTF-32 encodings), together with functions for Unicode
characters (character names, classifications, properties) and functions
for string processing (formatted output, width, word breaks, line
breaks, normalization, case folding, regular expressions).
      </description>

      <install-name>libunistring09</install-name>

      <pkgname-base>TWWLIBUNISTRING09</pkgname-base>

      <version>0.9.3</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWLIBICONV113"
          subpkg="runtime">v&gt;=1.13.1</depend>
      </subpkg>
    </package-manager>
  </package>
</packages>
