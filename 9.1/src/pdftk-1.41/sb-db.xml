<?xml version="1.0"?>
<programs>
<!DOCTYPE programs SYSTEM "sb-db.dtd">
  <program name="pdftk" version="1.41" revision="2"
  systype="hppa*-hpux11*|*-linux*|*-solaris2.[89]|*-solaris2.1?">
    <build-name>${SB_PROG_NAME}-${SB_PROG_VER}</build-name>

    <install-name>${SB_PROG_NAME}14</install-name>

    <sources>
      <source checksum="7214230b937dc996d209c05b7b7b3518"
        checksum-type="md5" size="756574"
        path="src/pdftk-1.41.tar.bz2"/>
      <patch checksum="89e11e84d190963f16f7894905d70508"
        checksum-type="md5" size="7255"
        path="src/pdftk-1.41.gentoo-gcc43.patch"/>
      <patch checksum="3b2d4cc1c821171c6492be3d8b6444a5"
        checksum-type="md5" size="827"
        path="src/pdftk-1.41.gentoo-gcc44.patch"/>
      <patch checksum="2f497ff4f85bbe02b3b53ec58b24d43a"
        checksum-type="md5" size="9254"
        path="src/pdftk-1.41.tww.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" install-name="gcc44" add-path="prepend"
        systype="*-hpux*|*-solaris*">v&gt;=4.4.6</depend>
    </dependencies>

    <script-header>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  _makefile="Makefile.AIX" ;;
*-hpux*)
  _makefile="Makefile.HPUX" ;;
*-linux*)
  _makefile="Makefile.RedHat" ;;
*-solaris*)
  _makefile="Makefile.Solaris" ;;
esac
]]>
    </script-header>

    <build>
<![CDATA[
gmake -C pdftk -f ${_makefile}
]]>
    </build>

    <install>
<![CDATA[
${SB_PATH_INSTALL} -m 755 -o root -g 0 -d ${SB_INSTALL_PREFIX}/bin \
${SB_INSTALL_PREFIX}/man/man1

${SB_PATH_INSTALL} -m 755 -o root -g 0 pdftk/pdftk \
${SB_INSTALL_PREFIX}/bin
${SB_PATH_INSTALL} -m 644 -o root -g 0 debian/pdftk.1 \
${SB_INSTALL_PREFIX}/man/man1
]]>
    </install>

    <licenses>
      <license path="gpl-2.license"/>
    </licenses>

    <notes>
      <change from="1.40" to="1.41">
        <item><para>Fixed a bug that corrupted output PDF xref
tables</para></item>
        <item><para>Fixed a bug that prevented XFDF form data from
being passed to pdftk via stdin</para></item>
        <item><para>Commented out some unused code from
pdftk.cc</para></item>
      </change>

      <change from="1.12" to="1.40">
        <item><para>Added the stamp operation, a natural complement to
the existing background operation</para></item>
        <item><para>Added the page rotating patch. Tweaked the patch
so it handles a greater variety of input syntax (e.g.,
1-20evenE).</para></item>
        <item><para>Added the generate_fdf patch. I actually rewrote
the patch so it uses FDF features built into the iText
library.</para></item>
        <item><para>The fill_form operation can now take XFDF data as
well as FDF data.</para></item>
        <item><para>Added the drop_xfa option so pdftk could fill
forms created with newer versions of Acrobat or Adobe
Designer</para></item>
        <item><para>Added the keep_first_id and keep_final_id options
for more PDF fun</para></item>
        <item><para>Upgraded the iText library we use to itext-paulo
rev. 155.</para></item>
        <item><para>Fixed a bug that caused pdftk to create bloated
PDFs when input PDF pages had links on their pages</para></item>
        <item><para>Added License-Adobe.txt to the fonts folder, as
required for distribution of Adobe's AFM files</para></item>
      </change>
    </notes>

    <changelog>
      <change date="2010 November 11" revision="2"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Add support for RHEL 6</para></item>
      </change>
    </changelog>
  </program>
</programs>
