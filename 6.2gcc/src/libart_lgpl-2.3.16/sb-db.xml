<?xml version="1.0"?>
<programs>
  <program name="libart_lgpl" version="2.3.16" revision="1">
    <build-name>${SB_PROG_NAME}-${SB_PROG_VER}</build-name>

    <install-name>libart23</install-name>

    <sources>
      <source checksum="6bb13292b00649d01400a5b29a6c87cb"
        checksum-type="md5" size="265322"
        path="src/libart_lgpl-2.3.16.tar.bz2"/>
      <patch checksum="6c302fd9f033d1ededf26171f2dfb87b"
        checksum-type="md5" size="802695"
        path="src/libart_lgpl-2.3.16.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" add-path="prepend">v==3.3.2</depend>
      <depend program="perl" var="PERL">v==5.8.2</depend>
    </dependencies>

    <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com"
  LDFLAGS="-Wl,-brtl -Wl,-blibpath:/usr/lib"
  CC_LD_RT="-Wl,-blibpath:" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -msym -readonly_strings"
  CC_LD_RT="-Wl,-rpath," ;;
*-hpux*)
  CC="cc -Ae" CFLAGS="+O2 -z +ESlit +Onofltacc +DAportable \
+Oentrysched +Odataprefetch +Onolimit"
  CC_LD_RT="-Wl,+s,+b," ;;
*-irix6*)
  CC=cc CFLAGS="-Wl,-woff,84 -Wl,-woff,85 -woff 1429 -O2 \
-OPT:Olimit=0"
  CC_LD_RT="-Wl,-rpath," ;;
*-linux*)
  CC_LD_RT="-Wl,-rpath," ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8"
  CC_LD_RT="-R" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xstrconst -xO2 -xtarget=generic"
  CC_LD_RT="-R" ;;
esac

./configure CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" \
${LDFLAGS+LDFLAGS="${LDFLAGS}"} --disable-dependency-tracking \
--prefix=${SB_INSTALL_PREFIX}

${SB_VAR_PERL}/bin/perl -pi -e "\
s!-L(\\\${exec_prefix}/lib)!\"-L\$1 ${CC_LD_RT}\$1\"!g" libart-config

${SB_VAR_PERL}/bin/perl -pi -e "\
s!-L(\\\${libdir})!-L\$1 ${CC_LD_RT}\$1!g" libart-2.0.pc
]]>
    </configure>
  </program>
</programs>
