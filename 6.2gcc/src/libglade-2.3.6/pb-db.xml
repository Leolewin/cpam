<?xml version="1.0"?>
<packages>
  <package name="libglade" version="2.3.6">
    <package-manager name="pkgadd">
      <category>System Environment/Libraries</category>
      <title>Glade Interface Library loader</title>
      <vendor>James Henstridge</vendor>

      <description attribution="freshmeat.net">
Libglade is a small library that allows a program to load its user
interface from an XML description at runtime. The XML file format is
that of the user interface builder GLADE, so libglade acts as an
alternative to GLADE's code generation approach. Libglade also
provides a simple interface for connecting handlers to the various
signals in the interface (on platforms where the gmodule library works
correctly, it is possible to connect all the handlers with a single
function call). Once the interface has been instantiated, libglade
gives no overhead, so other than the initial interface loading time
(which is short), there is no performance tradeoff.
      </description>

      <install-name>libglade23</install-name>

      <pkgname-base>TWWlgl23</pkgname-base>

      <version>2.3.6</version>
      <revision>1</revision>

      <subpkg type="runtime">
        <depend pkgname-base="TWWgc332"
          title="GNU Compiler Collection"
          subpkg="libruntime">v&gt;=3.3.2</depend>
        <depend pkgname-base="TWWgtk24"
          title="GTK+ Library"
          subpkg="runtime">v&gt;=2.4.4</depend>
        <depend pkgname-base="TWWxml26"
          title="XML C Library"
          subpkg="runtime">v&gt;=2.6.11</depend>
      </subpkg>

      <replace pkgname-base="TWWlgl20"
        install-name="libglade20"/>
    </package-manager>
  </package>
</packages>
