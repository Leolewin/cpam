<?xml version="1.0"?>
<programs>
  <program name="pkgconfig" version="0.15.0" revision="3">
    <build-name>${SB_PROG_NAME}-${SB_PROG_VER}</build-name>

    <install-name>${SB_PROG_NAME}</install-name>

    <sources>
      <source checksum="a7e4f60a6657dbc434334deb594cc242"
        checksum-type="md5" size="610697"
        path="src/pkgconfig-0.15.0.tar.gz"/>
      <patch checksum="ec083488d61b0762ac3c8fdfdf7aad4a"
        checksum-type="md5" size="577138"
        path="src/pkgconfig-0.15.0.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" add-path="prepend">v==3.3.2</depend>
    </dependencies>

    <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com" ;;
*-hpux10*)
  CC="cc -Ae" CFLAGS="+O2 -z +ESlit +Onofltacc +DAportable \
+Oentrysched +Odataprefetch +Onolimit"
  ARGS="--disable-threads" ;;
*-hpux*)
  CC="cc -Ae" CFLAGS="+O2 -z +ESlit +Onofltacc +DAportable \
+Oentrysched +Odataprefetch +Onolimit" ;;
*-irix6*)
  CC=cc CFLAGS="-Wl,-woff,84 -Wl,-woff,85 -woff 1429 -O2 \
-OPT:Olimit=0" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -msym -readonly_strings" ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xstrconst -xO2 -xtarget=generic" ;;
esac

rm -f config.cache
CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" ./configure \
--enable-shared --enable-static ${ARGS+${ARGS}} \
--prefix=${SB_INSTALL_PREFIX}
]]>
    </configure>

    <changelog>
      <change date="2004 August 26" revision="3"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Upgrade to latest libtool-1.4.2</para></item>
      </change>

      <change date="2004 August 1" revision="2"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Quote PKG_CHECK_MODULES definition in
pkg.m4 for Automake 1.8</para></item>
      </change>

      <change date="2004 January 6" revision="1"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Disable compatibility mode by searching in legacy
*-config scripts</para></item>
      </change>
    </changelog>
  </program>
</programs>
