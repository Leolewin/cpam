<?xml version="1.0"?>
<programs>
  <program name="expat" version="1.95.6" revision="1">
    <build-name>${SB_PROG_NAME}-${SB_PROG_VER}</build-name>

    <sources>
      <source checksum="ca78d94e83e9f077b5da2bfe28ba986a"
        checksum-type="md5" size="291876"
        path="src/expat-1.95.6.tar.gz"/>
      <patch checksum="db79b5d5a0e1fe94f4b8a682f674e211"
        checksum-type="md5" size="881259"
        path="src/expat-1.95.6.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" add-path="prepend">v==3.3.2</depend>
    </dependencies>

    <module name="default">
      <install-name>libexpat</install-name>

      <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com"
  LDFLAGS="-Wl,-brtl" ;;
*-hpux*)
  CC=cc CFLAGS="+O2 -Ae -z +DAportable +Oentrysched \
+Odataprefetch +Onolimit" ;;
*-irix6*)
  CC=cc CFLAGS="-Wl,-woff,84 -O2 -OPT:Olimit=0" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -std1 -readonly_strings" ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xstrconst -xO2 -xtarget=generic" ;;
esac

./configure CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" \
LDFLAGS="${LDFLAGS+${LDFLAGS}}" --enable-shared \
--enable-static --prefix=${SB_INSTALL_PREFIX}
]]>
      </configure>
    </module>

    <module name="tww-static"
    systype="*-osf4*">
      <install-name>libexpats</install-name>

      <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com"
  LDFLAGS="-Wl,-brtl" ;;
*-hpux*)
  CC=cc CFLAGS="+O2 -Ae -z +DAportable +Oentrysched \
+Odataprefetch +Onolimit" ;;
*-irix6*)
  CC=cc CFLAGS="-Wl,-woff,84 -O2 -OPT:Olimit=0" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -std1 -readonly_strings" ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xstrconst -xO2 -xtarget=generic" ;;
esac

./configure CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" \
LDFLAGS="${LDFLAGS+${LDFLAGS}}" --disable-shared --enable-static \
--prefix=${SB_INSTALL_PREFIX}
]]>
      </configure>
    </module>

    <changelog>
      <change date="2003 March 30" revision="1"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Add support for AIX 5.1 (same as AIX 
4)</para></item>
      </change>
    </changelog>
  </program>
</programs>
