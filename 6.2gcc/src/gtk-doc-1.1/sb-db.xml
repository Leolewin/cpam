<?xml version="1.0"?>
<programs>
  <program name="gtk-doc" version="1.1" revision="1"
  systype="*-hpux11*|*-irix*|*-linux*|*-osf*|*-solaris*">
    <build-name>${SB_PROG_NAME}-${SB_PROG_VER}</build-name>

    <install-name>gtkdoc11</install-name>

    <sources>
      <source checksum="add8e01da624025b2835f5f85a864e7f"
        checksum-type="md5" size="131682"
        path="src/gtk-doc-1.1.tar.bz2"/>
      <patch checksum="ce90d2ec2c8c236bcf11db6bc3a489e7"
        checksum-type="md5" size="77626"
        path="src/gtk-doc-1.1.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" add-path="prepend">v==3.3.2</depend>
      <depend program="openjade" install-name="openjade13"
        add-path="prepend">v&gt;=1.3.2</depend>
      <depend program="libxml" install-name="libxml25"
        add-path="prepend">v&gt;=2.5.11</depend>
      <depend program="libxslt" install-name="libxslt102"
        add-path="prepend">v&gt;=1.0.33</depend>
      <depend var="XMLCATALOG" install-name="xmlcatalog10"
        type="build"/>
      <depend program="perl" add-path="prepend"
        type="build">v==5.8.2</depend>
    </dependencies>

    <script-header>
<![CDATA[
_datadir="${SB_INSTALL_PREFIX}/share"
]]>
    </script-header>

    <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com" ;;
*-hpux*)
  CC="cc -Ae" CFLAGS="+O2 -z +ESlit +Onofltacc +DAportable \
+Oentrysched +Odataprefetch +Onolimit" ;;
*-irix6*)
  CC=cc CFLAGS="-Wl,-woff,84 -Wl,-woff,85 -woff 1429 -O2 -OPT:Olimit=0" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -std -readonly_strings" ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xstrconst -xO2 -xtarget=generic" ;;
esac

./configure CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" \
--datadir="${_datadir}" \
--with-xml-catalog=${SB_VAR_XMLCATALOG}/catalog.xml \
--prefix=${SB_INSTALL_PREFIX}
]]>
    </configure>

    <notes>
      <change from="1.0" to="1.1">
        <item><para>Add a gtk-doc.m4 macro that allows packages to
provide consistent checking for gtk-doc</para></item>
        <item><para>Check to make sure that the XML catalog actually
contains entries for the DocBook XML DTD and XSLT
stylesheets</para></item>
        <item><para>Add infrastructure for including the standard
gtk-doc makefile glue, so that maintainers of packages don't need to
worry about keeping it up to date</para></item>
        <item><para>Some updates to the devhelp contents file
generation</para></item>
        <item><para>Fix some bugs in the DocBook XML codepath that
were preventing inter-module cross references from being
resolved</para></item>
        <item><para>Fix some bugs in extraction of object property
documentation</para></item>
      </change>
    </notes>
  </program>
</programs>
