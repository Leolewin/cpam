<?xml version="1.0"?>
<programs>
  <program name="flex" version="2.5.4a" revision="1">
    <build-name>${SB_PROG_NAME}-2.5.4</build-name>

    <install-name>${SB_PROG_NAME}</install-name>

    <sources>
      <source checksum="bd8753d0b22e1f4ec87a553a73021adf"
        checksum-type="md5" size="380995"
        path="src/flex-2.5.4a.tar.gz"/>
      <patch checksum="6c3c6dadc1e42019bb63bd0ad6123af6"
        checksum-type="md5" size="328"
        path="src/flex-2.5.4a.patch"/>
    </sources>

    <dependencies>
      <depend program="gcc" add-path="prepend">v==3.3.2</depend>
      <depend program="bison" install-name="bison13"
        add-path="prepend" type="build">v&gt;=1.35</depend>
    </dependencies>

    <configure>
<![CDATA[
case "${SB_SYSTYPE}" in
*-aix*)
  CC=xlc CFLAGS="-O2 -qmaxmem=-1 -qarch=com" ;;
*-osf*)
  CC=cc CFLAGS="-O2 -std1" ;;
*-hpux*)
  CC=cc CFLAGS="+O2 -Ae -z +ESlit +Onofltacc +DAportable \
+Oentrysched +Odataprefetch +Onolimit" ;;
*-irix6*)
  CC=cc CFLAGS="-O2 -OPT:Olimit=0" ;;
*-solaris2.[89])
  CFLAGS="-O2 -mcpu=v8" ;;
*-solaris2*)
  CC=cc CFLAGS="-mr -Qn -xO2 -xtarget=generic" ;;
esac

CC="${CC:-gcc}" CFLAGS="${CFLAGS:--O2}" ./configure \
--prefix=${SB_INSTALL_PREFIX}
]]>
    </configure>

    <changelog>
      <change date="2003 April 4" revision="1"
      author="Albert Chin-A-Young" email="china@thewrittenword.com">
        <item><para>Add support for AIX 5.1 (same as AIX 
4)</para></item>
      </change>
    </changelog>
  </program>
</programs>
